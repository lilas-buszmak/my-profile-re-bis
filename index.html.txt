<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=DM+Sans:wght@400;500&display=swap" rel="stylesheet">
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

      /* ── Page background ── */
      body {
        font-family: 'DM Sans', sans-serif;
        background-color: #f0ede8;
        color: #1a1a1a;
      }

      /* ── Container: centers page content ── */
      .container {
        width: 700px;
        margin: 40px auto;
      }

      /* ── White cards ── */
      .card-white {
        background: #ffffff;
        border-radius: 6px;
        box-shadow: 0 4px 18px rgba(0, 0, 0, 0.08);
        padding: 36px 40px;
        margin-bottom: 16px;
        transition: box-shadow 0.25s, transform 0.25s;
      }

      .card-white:hover {
        box-shadow: 0 8px 30px rgba(0, 0, 0, 0.13);
        transform: translateY(-2px);
      }

      /* ── Profile picture: round ── */
      .img-circle {
        width: 110px;
        height: 110px;
        border-radius: 50%;
        object-fit: cover;
        border: 3px solid #1a1a1a;
        display: block;
        margin-bottom: 20px;
      }

      /* ── Text utilities ── */
      .text-center {
        text-align: center;
      }

      /* ── Headings ── */
      h1 {
        font-family: 'Playfair Display', serif;
        font-size: 2.4rem;
        line-height: 1.2;
        margin-bottom: 16px;
      }

      h2 {
        font-family: 'Playfair Display', serif;
        font-size: 1.7rem;
        color: #8e44ad;
        margin-bottom: 12px;
        display: flex;
        align-items: center;
        gap: 10px;
      }

      /* ── Paragraphs ── */
      p {
        font-size: 1rem;
        line-height: 1.75;
        margin-bottom: 20px;
      }

      /* ── Button link ── */
      .btn-red {
        display: inline-block;
        padding: 10px 22px;
        background-color: #c0392b;
        color: #ffffff !important;
        text-decoration: none;
        border-radius: 4px;
        font-weight: 500;
        font-size: 0.95rem;
        transition: background-color 0.2s;
      }

      .btn-red:hover {
        background-color: #a93226;
        opacity: 1;
      }

      /* ── Regular links ── */
      a {
        color: #c0392b;
        text-decoration: underline;
        text-underline-offset: 3px;
        transition: opacity 0.2s;
      }

      a:hover { opacity: 0.7; }

      /* ── Section divider ── */
      .section-divider {
        border: none;
        border-top: 1.5px solid #ececec;
        margin: 0 0 24px;
      }

      /* ── Inline social list ── */
      .list-inline {
        list-style: none;
        padding-left: 0;
      }

      .list-inline > li {
        display: inline;
        padding: 0 20px;
      }

      .list-inline > li:first-child {
        padding-left: 0;
      }

      .list-inline > li a {
        font-size: 1rem;
        font-weight: 500;
        text-decoration: none;
        color: #1a1a1a;
        display: inline-flex;
        align-items: center;
        gap: 7px;
        transition: color 0.2s;
      }

      .list-inline > li a:hover {
        color: #c0392b;
        opacity: 1;
      }

      .list-inline > li a i {
        font-size: 1.15rem;
      }

      /* ── Footer title ── */
      #footer-title {
        font-family: 'Playfair Display', serif;
        font-size: 1.4rem;
        margin-bottom: 18px;
      }
    </style>
  </head>
  <body>

    <!-- ══ CONTAINER ══ -->
    <div class="container">

      <!-- ── Card 1 · Introduction ── -->
      <div class="card-white">
        <h1>Hello, I'm Lilas 😎</h1>

        <img
          class="img-circle"
          src="C:\Users\lilas\Desktop\my-profile\images\photo moi.jpg"
          alt="Lilas' profile picture"
        >

        <p>
          I love Bad Bunny, traveling, discovering new cultures and running
          <i class="fa-solid fa-person-running"></i>.
          I came to IESEG because I love learning about new cultures and speaking English.
        </p>

        <a class="btn-red" href="https://www.ieseg.fr/" target="_blank">
          Discover IESEG →
        </a>
      </div>

      <!-- ── Card 2 · Bad Bunny ── -->
      <div class="card-white">
        <hr class="section-divider">
        <h2>Bad Bunny <span><i class="fa-solid fa-carrot"></i></span></h2>
        <p>
          I am passionate about <strong>Bad Bunny</strong>. Listening to his music
          takes me to the other side of the world — I want to learn to dance salsa
          and learn Spanish. It's just <em>amazing</em>.
        </p>
      </div>

      <!-- ── Card 3 · Travel ── -->
      <div class="card-white">
        <hr class="section-divider">
        <h2>Travel <span><i class="fa-solid fa-plane"></i></span></h2>
        <p>
          I am passionate about <strong>Traveling</strong>. Going anywhere
          and discovering new cultures — in Asia, Europe, Africa, Latin America.
          Every trip changes how I see the world.
        </p>
      </div>

      <!-- ── Card 4 · Running ── -->
      <div class="card-white">
        <hr class="section-divider">
        <h2>Running <span><i class="fa-solid fa-person-running"></i></span></h2>
        <p>
          I love <strong>Running</strong> without any real goal in mind,
          just to let off steam and breathe in the fresh air.
          Whether in the sun or in the rain, as long as I'm out in nature.
        </p>
      </div>

      <!-- ── Card 5 · Footer / Social links ── -->
      <div class="card-white text-center">
        <p id="footer-title">Follow me</p>
    <ul class="social-list">
      <li>
        <a href="https://www.instagram.com/" target="_blank">
          <i class="fa-brands fa-instagram"></i> Instagram
        </a>
      </li>
      <li>
        <a href="https://x.com/X" target="_blank">
          <i class="fa-brands fa-x-twitter"></i> X
        </a>
      </li>
      <li>
        <a href="https://www.tiktok.com/fr/"_blank">
          <i class="fa-brands fa-tiktok"></i>Tiktok
        </a>
      </li>
    </ul>
      </div>

    </div>
    <!-- ══ END CONTAINER ══ -->

  </body>
</html>